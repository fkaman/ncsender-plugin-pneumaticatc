⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## What's Changed

### 🔧 Improvements
- Tool length offsets measured during a touch-off probe are now saved back to the tool library automatically, so measured lengths persist between sessions instead of needing to be re-entered by hand
