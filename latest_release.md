⚠ claude.ai connectors are disabled because ANTHROPIC_API_KEY or another auth source is set and takes precedence over your claude.ai login · Unset it to load your organization's connectors
## What's Changed

### ✨ New Features
- Added manual dialogs for tool release and clamp, so you can trigger the ATC air actions directly without running a tool change
- New Automatic Release and Automatic Clamp settings let you choose whether the collet releases and clamps on its own during a tool change
